public class Circle implements Drawable {
    private double radius;

    public Circle(double radius){
        this.radius = radius;
    }
    public void draw(){
        System.out.println("Draw a Circle " + radius);
    }
    public double getArea(){
        return Math.PI * radius * radius;
    }
